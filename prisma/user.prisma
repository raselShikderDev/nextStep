model User {
  id           String  @id @default(uuid())
  name         String
  email        String  @unique
  phone        String? @unique
  passwordHash String
 // otherInfo
  role     Role    @default(USER)
  isActive Boolean @default(true)

  //   requests     ServiceRequest[]
  @@index([email])
}
